import React from 'react';
import LoginForm from '../components/LoginForm';

const Home: React.FC = () => {
    return (
        <div>
            <LoginForm />
        </div>
    );
};

export default Home;