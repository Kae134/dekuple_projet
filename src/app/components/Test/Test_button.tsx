'use client';
import React from "react";

const Button = () => {
    return (
        <div>
            <button onClick={() => console.log('Test')}>test</button>
        </div>
    )
}

export default Button